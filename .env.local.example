# .env.local.example
# Server-side only — used by /api/predict and /api/health proxy routes.
# NEVER prefix this with NEXT_PUBLIC_. Keeping it server-only is exactly what
# lets the browser avoid mixed-content/CORS issues with the plain-HTTP backend.
BACKEND_URL=http://13.205.125.147